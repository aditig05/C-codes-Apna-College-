#include <iostream>
using namespace std;
// inverted half pyramid
int main() {
    int n;
    for(n=5; n>=1; n--){
        for(int i=1 ; i<=n; i++){
            cout<<"*";
            }
       cout<<endl; //to break the line and go to the next line
    }   
	return 0;
}
